# http → hyper text transfer protocol
http is the application layer protocol, used for communication between client and server.

# TCP/IP → transmission control protocol/internet protocol
TCP/IP defins how data is transmitted from client to server accross networks.